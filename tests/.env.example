# Copy to .env and fill in. Only OLLAMA_API_KEY is required for the
# classify_intent scenarios in chat_context_followup.py, since that
# function only calls the Ollama Cloud API directly, no database needed.
OLLAMA_API_KEY=
